import { ComingSoon } from '@/components/ComingSoon';

export default function ChatTab() {
  return (
    <ComingSoon
      titel="Werfchat"
      tekst="Chat per werf met foto's en plannen in dezelfde lijn komt in de volgende fase. Voor nu: deel foto's en updates via het werfrapport."
    />
  );
}
